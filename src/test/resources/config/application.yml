# Spring application
spring :
  main :
    webEnvironment : false
    showBanner     : false

# Web Drivers   
minium:
  webdriver:
    desiredCapabilities:
      browserName: chrome
    window:
      maximized: true

# Configuration that can be injected in your tests
  config:
    doodleId: 776qtpf6vqm9tfi5
